# Leave unset for local dev — Vite proxies /api/* to localhost:3001 (see vite.config.js).
# In production (e.g. Vercel), set this to your deployed server's URL, no trailing slash.
VITE_API_URL=https://your-server.onrender.com
